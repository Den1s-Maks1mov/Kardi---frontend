import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * Інтерфейс, що описує декомпозиційну структуру контенту для компонента підказки.
 * Заміна єдиного сирого HTML-рядка на масив `paragraphs` нівелює ризики XSS-вразливостей,
 * оскільки виключає використання директиви `innerHTML` на користь безпечної інтерполяції.
 */
export interface TooltipContent {
  title: string;
  paragraphs: string[];
  alertText?: string; // Опціональний маркер критичного попередження для семантичного виділення (важливі медичні застереження)
}

/**
 * Статична константна таблиця відповідностей (Lookup Table / Record Dictionary).
 * Зберігає структуровані бізнес-дані інформаційних підказок, де ключем виступає строковий токен rPPG-статусу.
 * Використання типу `Record<string, TooltipContent>` гарантує строгу типізацію словника.
 */
export const TOOLTIP_DATA: Record<string, TooltipContent> = {
  history: {
    title: 'Історія',
    paragraphs: ['Усі ваші попередні вимірювання за останні 30 днів']
  },
  stats: {
    title: 'Тижнева статистика',
    paragraphs: ['Мінімальний, середній та максимальний пульс за останні 7 днів']
  },
  low: {
    title: 'Низький (<60)',
    paragraphs: [
      'Ваш пульс нижчий за стандартну норму.',
      'Чи це ОК: Залежить від контексту. Якщо ви професійний спортсмен або перебуваєте у глибокому сні, це може бути нормою для вашого організму.',
      'Але, якщо ви відчуваєте слабкість, сильну втому, запаморочення, задишку чи біль у грудях, або ви відчуваєте напівнепритомний стан, обов\'зательно зверніться за медичною допомогою.'
    ]
  },
  normal: {
    title: 'Норма (60 - 100)',
    paragraphs: [
      'Для більшості здорових дорослих людей нормальний пульс у стані спокою становить від 60 до 100 ударів на хвилину.',
      'Ваш результат свідчить про здоровий та розслаблений стан серцево-судинної системи. Турбуватися немає про що.'
    ]
  },
  warning: {
    title: 'Межа (100 - 130)',
    paragraphs: [
      'Ваше серце б’ється помітно швидше за норму. Такий стан прискореного серцебиття називається тахікардією.',
      'Чи це Ок: Залежить від контексту. Це ОК, якщо ви щойно після легкої пробіжки, піднімалися сходами, відчуваєте сильне хвилювання (стрес) або випили багато кави.',
      'Це НЕ ОК, якщо ви перебуваєте у стані спокою (просто сидите чи лежите). Якщо такий пульс тримається довго без фізичних навантажень, варто відпочити та поспостерігати за станом.'
    ]
  },
  danger: {
    title: 'Відхилення (130 - 140+)',
    paragraphs: [
      'Дуже висока частота серцевих скорочень. Ваше серце зараз працює на високих обертах.',
      'Якщо ви не займалися спортом, а пульс підскочив до 165 bpm, негайно присядьте, спробуйте глибоко та повільно подихати. Якщо пульс не падає, або є біль у грудях чи запаморочення — '
    ],
    alertText: 'обов\'язково зверніться за медичною допомогою.'
  }
};

@Component({
  selector: 'app-tooltip',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './tooltip.html',
  styleUrls: ['./tooltip.scss']
})
export class TooltipComponent {
  /** Вхідні параметри (API компонента) для декларативного зв'язування даних (Property Binding) */
  @Input() title = '';
  @Input() paragraphs: string[] = [];
  @Input() alertText = '';
}
